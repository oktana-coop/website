---
interface Props {
  text: string;
  classes?: string;
}

const { text, classes } = Astro.props;
---

<h2
  class=`text-2xl sm:text-3xl md:text-4xl font-serif font-bold leading-none text-center ${classes ?? ''}`
>
  {text}
</h2>
